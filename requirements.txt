# Runtime requirements entrypoint for source installs.
#
# Target baseline:
# - Red Hat Enterprise Linux 8.10
# - glibc 2.28
# - Python 3.12.x
#
# For dev/test tooling use:
#   python -m pip install -r requirements/rhel8-dev.txt
#
# For offline target installs, use this file only with a prebuilt wheelhouse:
#   python -m pip install --no-index --find-links wheelhouse -r requirements.txt
-r requirements/rhel8-runtime.txt
